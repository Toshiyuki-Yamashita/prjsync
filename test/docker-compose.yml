version: '3.1'
services:
  redmine:
    image: redmine:2.6
    ports:
      - 8081:3000
    volumes:
      - redmine:/usr/src/redmine/files
      - redmine:/usr/src/redmine/log
      - redmine:/usr/src/redmine/sqlite
volumes:
  redmine:
    driver: local

